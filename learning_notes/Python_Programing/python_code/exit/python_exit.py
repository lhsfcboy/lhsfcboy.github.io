https://stackoverflow.com/questions/73663/terminating-a-python-script

built-in function quit(). There is no need to import any library, and it is efficient and simple.

Example:

#do stuff
if this == that:
  quit()

only terminates the script which spawned it
