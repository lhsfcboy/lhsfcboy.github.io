# 交通模拟

## 道路增加, 反而堵塞

条件:

- 每个车只记得自己的最近几次选择
- 并不知道其他车的运行结果
- 也没有外部的信息源

## 具体的模拟算法

- 设有三条道路 a, b, c
- 初始时, 给所有道路记0分
  - Q_a = 0, Q_b = 0, Q_c = 0
- 此时选择每条道路的概率相同
  - P_a = P_b = P_c = 1/3
- 每次行走后, 更新打分
- 给新的打分当中的最高项的道路调整概率
  - P_a = P_a + delta, if Q_a is max